# Если выписать все натуральные числа меньше 10, кратные 3 или 5, то получим 3, 5, 6 и 9. Сумма этих чисел равна 23.
# Найдите сумму всех чисел меньше 1000, кратных 3 или 5.

# Функция для нахождения суммы чисел, кратных n, до m (не включая m)
def sum_multiples(n, m):
    # Находим количество чисел, кратных n, меньше m
    count = (m - 1) // n
    # Сумма чисел кратных n: n * (1 + 2 + ... + count) = n * (count * (count + 1)) // 2
    return n * count * (count + 1) // 2

# Задача: найти сумму чисел меньше 1000, кратных 3 или 5
m = 1000
sum_3 = sum_multiples(3, m)
sum_5 = sum_multiples(5, m)
sum_15 = sum_multiples(15, m)

# Итоговая сумма по принципу включений и исключений
result = sum_3 + sum_5 - sum_15
print (result)
